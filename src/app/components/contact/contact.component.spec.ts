describe('[TODO] ContactComponent', () => {
  // Test - przykładowy test, który wypisuje "TODO"
  it('test example', () => {
    console.log('TODO');
    const x = 1 + 1;
    expect(x).toBe(2);
    expect(x).toEqual(2);
  });
});
